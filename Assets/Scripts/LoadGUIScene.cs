using UnityEngine;
using System.Collections;

public class LoadGUIScene : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Application.LoadLevelAdditive(0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
